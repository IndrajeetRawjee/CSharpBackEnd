using System;

namespace NullableTypesTrain
{
    //What are Nullable types.
    //Values cannot  be null. But there might arise a case where you need value as null. Here nullable types are used
    // ?? Null Qualicy operator --- Var = Value ?? DefaultValueOnNull
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
